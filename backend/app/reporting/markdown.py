"""
README 4.5 / ТЗ раздел 3.4: выгрузка отчёта в Markdown. Вызывается из routers/reports.py
(GET /repos/{repo_id}/report) — не дублировать логику сборки данных, только рендер готового
RepositoryDetail (backend/app/schemas/analysis.py) в текст.

Целевой вид результата (см. пример репозитория в ТЗ 3.4):

    # awesome-project

    Repo Health Score: 74/100 — Хорошее состояние, есть точки роста
    Ссылка: https://sourcecraft.dev/org/awesome-project
    Дата анализа: 2026-09-17

    ## Категории

    - Documentation: 45/100 — отсутствует инструкция локального запуска
    - CI/CD: 82/100 — 23% последних прогонов завершились неуспешно
    - Security: 61/100 — обнаружены две критические уязвимости в зависимостях
    - Activity: 88/100 — проект активно развивается
    - Issues: 76/100 — две открытые задачи не обновлялись более 30 дней
    - Code health: 70/100 — найдено 47 TODO/FIXME

    ## Рекомендации

    - [HIGH] Обновите зависимости с критическими уязвимостями

Для категории со status == "NO_DATA" — писать "Нет данных" вместо числа (тот же текст,
что и в frontend/src/components/CategoryCard.tsx — одна и та же формулировка везде).
Если рекомендаций нет — писать "Критичных рекомендаций нет".

Данных для проверки пока ни у кого нет (остальная часть бэкенда не готова) — собрать
RepositoryDetail с придуманными значениями самостоятельно, см. пример выше, и проверить,
что функция возвращает такой же текст, просто со своими числами.
"""

from ..schemas.analysis import RepositoryDetail


def render_markdown_report(detail: RepositoryDetail) -> str:
    raise NotImplementedError
